
This is a Maven project. To start the Application:
mvn spring-boot:run

To test and build fat jar:
mvn package

To run jar:
java -jar target/orch-1.0-SNAPSHOT.jar

Too many options for Documentation -- skipped.